name: Packer

on:
  pull_request:
    branches:
      - master

jobs:
  echo-input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          script: |
                echo "Fetching packer"
               